import React, { View } from "react-native";

interface Props {
  setShowIntroduceScreen: (on: boolean) => void;
}
const LoadingPage = ({ setShowIntroduceScreen }: Props) => {
  return <View></View>;
};

export default LoadingPage;
