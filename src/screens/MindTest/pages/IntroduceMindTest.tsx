import React, { View } from "react-native";

interface Props {
  setShowIntroduceScreen?: (on: boolean) => void;
}
const IntroduceMindTest = ({ setShowIntroduceScreen }: Props) => {
  return <View></View>;
};

export default IntroduceMindTest;
